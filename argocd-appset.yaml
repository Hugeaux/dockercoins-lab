apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: dockercoins-lab
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - git:
              repoURL: https://github.com/Hugeaux/dockercoins-lab.git
              revision: main
              directories:
                - path: deployments/*
          - list:
              elements:
                - name: webui
                  port: "80"
                  imageName: jpetazzo/dockercoins-webui
                - name: worker
                  port: "80"
                  imageName: jpetazzo/dockercoins-worker
                - name: hasher
                  port: "80"
                  imageName: jpetazzo/dockercoins-hasher
                - name: rng
                  port: "80"
                  imageName: jpetazzo/dockercoins-rng
                - name: redis
                  port: "6379"
                  imageName: redis
  template:
    metadata:
      name: '{{path.basename}}-{{name}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/Hugeaux/dockercoins-lab.git
        targetRevision: main
        path: charts/generic-service
        helm:
          valueFiles:
            - '../../{{path}}/values.yaml'
          values: |
            nameOverride: {{name}}
            service:
              port: {{port}}
            # FIX: Nest the repository under 'image' to satisfy the chart
            image:
              repository: {{imageName}}
              tag: {{tag | default "latest"}}
      destination:
        server: https://kubernetes.default.svc
        namespace: 'dockercoins-{{path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
